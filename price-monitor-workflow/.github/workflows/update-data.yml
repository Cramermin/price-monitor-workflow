name: Daily Data Update

on:
  schedule:
    - cron: '0 2 * * *'  # 每天 UTC 2:00 (北京时间 10:00)
  workflow_dispatch:      # 允许手动触发

# --- 👇 核心修复：这一行必须顶格写，前面不能有空格！👇 ---
permissions:
  contents: write
# -----------------------------------------------------

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install pandas numpy

    - name: Generate mock data
      # 这里的路径对应你截图里的文件结构 src/data/data_generator.py
      run: |
        python src/data/data_generator.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions'
        git config --global user.email 'github-actions@github.com'
        # 你的 Python 脚本是把文件存在 data/historical_prices.csv 的，所以这里路径是对的
        git add data/historical_prices.csv
        git commit -m "Update simulated price $(date '+%Y-%m-%d')" || echo "No changes to commit"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
